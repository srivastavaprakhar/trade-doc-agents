# Long-lived Claude Code OAuth token. Generate once with:  claude setup-token
# (requires the Claude Code CLI installed and logged into your Claude subscription)
# Treat this like a password: never commit it, never share it.
CLAUDE_CODE_OAUTH_TOKEN=
